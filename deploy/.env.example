# Copy to deploy/.env (git-ignored) on the VM and fill in.
#
# Cloudflare Tunnel token — Zero Trust dashboard -> Networks -> Tunnels ->
# create a tunnel (choose "Cloudflared") -> copy the token from the install
# command (the long string after `--token`). One tunnel, one token.
TUNNEL_TOKEN=
